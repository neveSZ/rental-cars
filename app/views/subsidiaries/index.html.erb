<% if (@subsidiaries.length > 0) %>
    <% @subsidiaries.each do |subsidiary| %>
        <%= link_to subsidiary.name, subsidiary_path(id: subsidiary.id) %>
    <% end %>
<% else %>
    Nenhuma filial cadastrada
<% end %>
<%= link_to 'Voltar', root_path %>